qqline(sample, distribution = qcauchy)
# Add grid to the first plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
par(mar=c(5, 0.5, 4, 4))
# Create the second plot without a y-axis
plot(theoretical_quantiles_norm, sample, xlab = "Quantis Teóricos", ylab = "", yaxt = "n")
qqline(sample)
# Add grid to the second plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2))
# Create the first plot
par(mar=c(5, 4, 4, 0.5))
plot(theoretical_quantiles_cauchy, sample, xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
qqline(sample, distribution = qcauchy)
# Add grid to the first plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
par(mar=c(5, 0.5, 4, 4))
# Create the second plot without a y-axis
plot(theoretical_quantiles_norm, sample, xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
qqline(sample)
# Add grid to the second plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2))
# Create the first plot
par(mar=c(5, 4, 4, 0.5))
# Add grid to the first plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
plot(theoretical_quantiles_cauchy, sample, xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
qqline(sample, distribution = qcauchy)
par(mar=c(5, 0.5, 4, 4))
# Create the second plot without a y-axis
plot(theoretical_quantiles_norm, sample, xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
qqline(sample)
# Add grid to the second plot
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy)
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red")
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red")
help(qqline)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",size=1)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red".lwd = 1)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 2)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1.5)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red",lwd = 1.5)
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1.5)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red",lwd = 1.5)
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1.5)
legend(1, 95, legend=c("Line 1", "Line 2"),
col=c("red", "blue"), lty=1:2, cex=0.8)
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2,3), ncol=3), widths=c(1,1,0.3), heights=c(1,1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample, pch = 16, col = "black")
qqline(sample, distribution = qcauchy, col = "red")
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample, pch = 16, col = "black")
qqline(sample, col = "red")
# Add a legend in the third plot space
par(mar=c(5, 0, 4, 1))
plot.new()
legend("center",
legend = c("Sample points", "Theoretical QQ line"),
col = c("black", "red"),
pch = c(16, NA),
lty = c(NA, 1),
bty = "n")
# Fixar a semente para a reprodutibilidade
set.seed(1501)
# Parâmetros da distribuição de Cauchy
location = -1.6
scale = 1
# Gerar a amostra
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Calcular os quantis teóricos
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, 3.6, 3.4)
# Ordenar a amostra
sample = sort(sample)
x = 1:sample_size
# Set up the layout
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins (bottom, left, top, right)
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red",lwd = 1.5)
# Adjust the margins for the second plot (bottom, left, top, right)
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1.5)
help(qnorm)
# Set designated seed
set.seed(1501)
# Cauchy's distribuition parameters
location = -1.6
scale = 1
# Normal's distribuition parameters
mean = 3.6
sd = 3.4
# Generate sample
sample_size = 178
sample = rcauchy(sample_size, location, scale)
# Find theoretical quantiles
quantiles = (1:sample_size) / (sample_size + 1)
theoretical_quantiles_cauchy = qcauchy(quantiles, location, scale)
theoretical_quantiles_norm = qnorm(quantiles, mean, sd)
# sort sample
sample = sort(sample)
x = 1:sample_size
# Set a specific layout for plot
layout(matrix(c(1,2), ncol=2), widths=c(1,1), heights=c(1,1))
# Adjust the margins for first plot
par(mar=c(5, 4, 4, 0.5))
# Create a blank plot with the grid
plot(theoretical_quantiles_cauchy, sample, type = "n", xlab = "Quantis Teóricos (Cauchy)", ylab = "Valores da Amostra")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_cauchy)), col = "gray", lty = "dashed")
# Add the points and line to the plot
points(theoretical_quantiles_cauchy, sample)
qqline(sample, distribution = qcauchy, col="red",lwd = 1.5)
# Adjust the margins for second plot
par(mar=c(5, 0.5, 4, 4))
# Create a blank plot with the grid for the second plot
plot(theoretical_quantiles_norm, sample, type = "n", xlab = "Quantis Teóricos (Normal)", ylab = "", yaxt = "n")
abline(h = pretty(range(sample)), v = pretty(range(theoretical_quantiles_norm)), col = "gray", lty = "dashed")
# Add the points and line to the second plot
points(theoretical_quantiles_norm, sample)
qqline(sample, col="red",lwd = 1.5)
